export type SearchModel = {
  data : searchObj[]
}

export type searchObj = {
  closeTime : string,
  deliveredTime : string,
  description : string,
  location : string,
  name : string,
  openTime : string,
  phoneTime : string,
  rating : number,
  resImg : string,
  resId : string,
}
